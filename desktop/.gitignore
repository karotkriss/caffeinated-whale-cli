# Frontend adherence-lint tooling deps (installed via `npm ci` in CI)
/node_modules/

# Rust / Tauri build artifacts
/src-tauri/target/
# Tauri-generated capability schemas (regenerated every build)
/src-tauri/gen/
!/dist/
!/dist/**
