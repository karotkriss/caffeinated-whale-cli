The health verdict compressed to a dot, for tree rows and tabs.

```jsx
<HealthDot status="running" />
```

- Always pair it with the instance name; a dot alone is not a label. Give it a `title` so the token is still readable.
