A toggle for a setting that persists — not for an action.

```jsx
<Switch checked={follow} onChange={...} label="Follow logs" />
```

- If the change runs a command, use a Button instead. A Switch only stores a preference.
