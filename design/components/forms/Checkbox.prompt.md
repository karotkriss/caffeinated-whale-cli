A checkbox, used above all for the consent gates destructive verbs carry.

```jsx
<Checkbox checked={vols} onChange={...} label="Also remove named volumes" hint="This deletes the database. There is no undo." />
```

- The hint states the consequence in plain words. It does not say "Warning:".
