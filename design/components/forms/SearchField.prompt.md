The one search affordance: it searches the inspect cache for apps and sites, the same source `cwcli where` reads.

```jsx
<SearchField value={q} onChange={e => setQ(e.target.value)} />
```

- Placeholder names what is searched, never a bare "Search".
