A tinted classifier: a tier name, a count, a version, a "v16".

```jsx
<Badge tone="accent">FAST</Badge>
<Badge tone="neutral" uppercase={false}>3 benches</Badge>
```

- A Badge classifies; it never states health. Use `StatusPill` for running/degraded/offline/online/unknown.
