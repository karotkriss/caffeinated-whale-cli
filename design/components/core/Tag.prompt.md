A monospace chip for a value the machine produced.

```jsx
<Tag icon="globe">8000-8005</Tag>
<Tag icon="git-branch">version-15</Tag>
<Tag icon="tag" mono={false}>staging</Tag>
```

- Ports, pids, paths, branches and site names are monospace. A human-typed bench label may use `mono={false}`.
