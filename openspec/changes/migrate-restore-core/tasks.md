## 1. Characterization first: restore's behavior pinned green BEFORE anything moves

- [x] 1.1 Run the two existing restore suites at the batch's base commit and record the baseline: `tests/test_restore_safety.py` (receive confirm/origin/password-off-argv/missing-apps + normal-path selectors/exit-codes) and `tests/test_restore_inspect_fixes.py` (the six restore+inspect bugs: shared site detection, default-site resolution, receive full-path, credential prompts, missing-apps, migrate+restart).
- [x] 1.2 Add `tests/test_restore_characterization.py` for the currently-thin command-level behaviors, green against UNMIGRATED code, committed separately (batch 4's discipline), through migration-surviving seams (container fakes, questionary, subprocess, `isatty`, `TipSpinner` no-op): the normal-path exec command string + arg ORDER (db path, `--mariadb-root-username`, `--mariadb-root-password "$CWCLI_MARIADB_ROOT_PASSWORD"`, `--force`, `--with-public-files`, `--with-private-files`) with the secret in `environment=` never in the argv; the receive-path exec built with the FULL container path (not the bare filename); the encryption-key merge writing `encryption_key` into `site_config.json`; the migrate-then-restart order and the migrate-failure exit 1; the destructive confirm + missing-apps gate on BOTH paths (`--yes` proceeds, non-TTY refuses exit 1, decline exits 1); the non-TTY-no-selector refusal; the `--latest`/`--backup-file` mutual exclusion and the send/receive exclusion; the streamed `put_archive`/`get_archive` copies; the origin-mismatch warning.

## 2. `core/restore.py`: the plan/apply slice under its own tests

- [x] 2.1 `RestorePlan` and `RestoreReport` frozen/slots/kw_only DTOs per design (plain strings/bools/lists; NO password field; container-path strings not handles; serializable via `asdict`).
- [x] 2.2 Move the pure helpers to the core unchanged: `parse_backup_filename`, `transform_site_name_to_backup_format`, `group_backup_sets`, `group_and_sort_backups`, `select_backup_set` (its multi-match `console.print` becomes a `Result.warnings` message), `scan_backups_for_all_sites`, `_read_backup_installed_apps`, `check_missing_apps` (with the deprecated `no_recache` no-op param), and the LIVE-fallback `_resolve_default_site` (kept private, Decision 8).
- [x] 2.3 `restore_plan(project_name, *, site=None, latest=False, backup_file=None, selected_backup=None, bench=None, bench_path=None, on_event=None) -> Result[RestorePlan]`: the `open_plan` fallback-populate bench resolution (`resolve_bench` -> inspect -> re-resolve -> default), `resolve_container_state(offer_choice=True)` for the run-state, default-site resolution, `validate_site_name`/`validate_bench_path`/`require_bench_dir`/`require_site_dir`, scan, the `selected_backup`/`latest`/`backup_file` selection else `NEEDS_CHOICE`/`select_backup` (candidate sets as option dicts), missing-apps + origin via the shared `_build_plan` tail; emits `RestoreStep` events for the scan/check spinners.
- [x] 2.4 `receive_plan(project_name, *, site=None, bench=None, bench_path=None, downloaded_files, on_event=None) -> Result[RestorePlan]`: ensure-running run-state, bench + default-site resolution, streamed `put_archive` copy-in (checked bool return, `PRECONDITION` on failure), `parse_backup_filename` identification (db required, else `PRECONDITION`), the shared `_build_plan` tail.
- [x] 2.5 `restore_apply(plan, *, mariadb_root_username, mariadb_root_password, admin_password=None, consent=False, no_migrate=False, on_event=None) -> Result[RestoreReport]`: `confirm_restore` `NEEDS_CHOICE` when `not consent`; username metachar guard; dump `test -f`; build the command with secrets in `environment=` (arg order byte-exact, both paths identical); buffered `exec_run`; `PRECONDITION` on a hard failure with today's banner in `detail`; the encryption-key merge helper; migrate (buffered, exception-tolerant) + `core.start(restart=True)`; `WARNING` + `migrate_ok=False` when migrate fails.
- [x] 2.6 `scan_backups(project_name, *, site=None, bench=None, bench_path=None, on_event=None) -> Result[...]` and `copy_backup_files_out(container, file_paths, dest_dir)` for the send path (scan/group reused; streamed `get_archive` copy-out).
- [x] 2.7 The typed event family (`RestoreStep`/`RestoreOutput`/`RestoreNotice` + `OnEvent`) and the module docstring recording the plan/apply read/destroy separation and the no-`axi restore` deferral.
- [x] 2.8 `tests/test_core_restore.py`: every branch above on container/subprocess fakes; both choice surfaces (`select_backup`, `confirm_restore`); the tri-mode selection matrix; secrets ride `environment=` and appear in NO event/DTO/warning; the exec arg-order preservation on both paths; the migrate-failure `WARNING`/`migrate_ok=False`; the streamed copies; `asdict`-is-plain-data; "the core prints nothing at all"; the origin mismatch.

## 3. Reseat `commands/restore.py` as a renderer

- [x] 3.1 Frontend keeps: the Typer signature, the mutual-exclusion errors, the questionary backup menu (rendered from `select_backup`, plus the frontend-only "Restore from remote source" affordance diverting to receive), the two confirms (rendered from plan facts, `--yes`/non-TTY-gated, `auto_enter=False`), `_prompt_mariadb_credentials` (unchanged, AFTER the confirms), the spinners driven by `RestoreStep` events, `handle_docker_errors`.
- [x] 3.2 The NEEDS_CHOICE-resolution loop (the `inspect` pattern): call `restore_plan` under the spinner; on `confirm_start` resolve via `ensure_containers_running` + re-invoke; on `select_bench` prompt + re-invoke; on `select_backup` render the menu OUTSIDE the spinner + re-invoke (or divert to receive on the remote affordance); on OK render the gates, collect credentials, call `restore_apply(consent=True)`; on `confirm_restore` the frontend already confirmed so this is the fail-closed backstop.
- [x] 3.3 The send/receive orchestration stays frontend (`ensure_sendme_installed`, the sendme send/receive subprocess, the ticket prompt, the clipboard, the Ctrl-C wait), calling the core `scan_backups`/`copy_backup_files_out` (send) and `receive_plan` -> `restore_apply` (receive).
- [x] 3.4 Re-point the two suites: pure-helper tests move with their subjects to `tests/test_core_restore.py`; the receive/normal Typer-command tests split BY DESIGN into core tests and frontend-render tests (named here when done); assertions otherwise untouched.
- [x] 3.5 Grep proves `commands/restore.py` no longer inline-owns the destructive exec, the scan, the missing-apps check, the encryption-key merge, or the bench no-cache fallback; the core owns them.

## 4. Asserted absences and dead code

- [x] 4.1 A test asserts the `axi` Typer registry has no `restore` command, its docstring recording DEFERRED (the captain's product decision) rather than never, per design Decision 9.
- [x] 4.2 Delete with their callers the frontend copies of every helper moved to the core; grep proves no caller remains for each.

## 5. Suite health

- [x] 5.1 The §1 characterization tests green against the MIGRATED code unchanged.
- [x] 5.2 Full fast tier green (`uv run pytest`); black, ruff, mypy at zero; `tests/README.md` coverage map updated.

## 6. E2E on a real instance, BOTH modes (captain destructive-delete standard)

- [x] 6.1 MEMORY-SERIALIZE first: `docker ps --format '{{.Names}}' | grep cwe2e` - if any non-mine `cwe2e-` container exists, append `paused: holding E2E for sibling cwe2e- instances to clear` and re-check on a slow cadence until gone.
- [x] 6.2 One throwaway isolated instance (`cwe2e-` prefix, `CWCLI_HOME` isolation per skill `cwcli-e2e-testing`), against the worktree's OWN editable install, reused throughout and torn down IMMEDIATELY at the end; no broad Docker cleanups; disk pressure means stop and report blocked.
- [x] 6.3 Real full lifecycle: `cwcli init` -> seed real data -> `cwcli backup` -> mutate the site -> `cwcli restore` and verify the restore is GENUINE (the seeded data is back, the DB is at the backup's state). Interactive (pty, `ESC[?2004h` awaited: menu select, `y`+Enter destructive confirm, credential prompts) AND non-interactive (`--latest --yes --mariadb-root-password`). Confirm migrate + restart ran and the site serves.
- [x] 6.4 Targeted legs: the non-TTY-no-selector refusal (exit 1); the destructive-decline exit 1; `--backup-file` selection; the origin-mismatch warning; `--no-migrate`; the `--send`/`--receive` exclusion and `--latest`/`--backup-file` exclusion (fast, no P2P peer needed).
- [ ] 6.5 Re-run AFTER the no-mistakes pipeline and after any review fixes (the standard: review fixes are a trigger, not an exemption).

## 7. Docs and memory

- [x] 7.1 CLAUDE.md ledger: `restore` moves to the migrated list with the plan/apply read/destroy split, the buffered-exec/secret-transport preservation, the `select_backup`/`confirm_restore` kinds, and the deferred-`axi restore` assertion; the un-migrated list restated from the code (`rm`, `self_update`'s mutating half).
- [x] 7.2 `cwcli-core-axi` skill: the batch section (the plan/apply headline finding, the two reported items, the deferral); `cwcli-lifecycle` skill's `references/restore.md`: re-pointed at the core seams, all six root-cause "why"s preserved.
- [x] 7.3 `docs/technical/README.md` core module list + `tests/README.md` updated; `docs/e2e/` gains the restore-migration evidence file; README checked for stale restore internals prose (user-facing behavior unchanged, so likely no edit).
